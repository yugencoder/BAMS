package com.ultimate.bams;

import com.ultimate.bams.R;

import android.app.Activity;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.util.Log;
import android.content.Intent;

public class Menu extends Activity {
	/*
	protected static final String TAG = null;
	Button b_register_student,
		   b_show_attendance,
		   b_send_data,
		   b_take_attendance,
		   b_register_fingerprints;

	protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_menu);
        Log.e(TAG, "Entering onCreate of Menu");
        
        b_register_student = (Button)findViewById(R.id.register_student);
        b_register_student.setOnClickListener(this);

        b_take_attendance = (Button) findViewById(R.id.take_attendance);
        b_take_attendance.setOnClickListener(this);

        b_show_attendance = (Button) findViewById(R.id.show_attendance);
        b_show_attendance.setOnClickListener(this);

	    
        b_register_fingerprints = (Button) findViewById(R.id.register_fingerprints);
        b_register_fingerprints.setOnClickListener(this);

	    
        b_send_data = (Button) findViewById(R.id.send_data);
        b_send_data.setOnClickListener(this);

	    
	}

	@Override
	public void onClick(View v) {
		 if (v == b_take_attendance) {
	        	Intent myintent = new Intent(getApplicationContext(), Take_Attendance.class);
	            startActivity(myintent);
	            Log.e(TAG, "Take Attendance");

	        }
		 
		 if (v == b_register_student) {
	        	Intent myintent = new Intent(getApplicationContext(), Register_Student.class);
	            startActivity(myintent);
	            Log.e(TAG, "Register Student");

	        }
		
		 if (v == b_show_attendance) {
	        	//Intent myintent = new Intent(getApplicationContext(), take_attendance_List.class);
	           // startActivity(myintent);
	            Log.e(TAG, "Show Attendance");

	        }
	   
		 if (v == b_send_data) {
	        	//Intent myintent = new Intent(getApplicationContext(), take_attendance_List.class);
	           // startActivity(myintent);
	            Log.e(TAG, "Sync Data");

	        }
		 if (v == b_register_fingerprints) {
	        	Intent in = new Intent(getApplicationContext(), Edit_Details_Activity.class);
	            startActivity(in);
	            Log.e(TAG, "Register Fingerprints");

	        }
		
		// TODO Auto-generated method stub
		
	}

  */
}
